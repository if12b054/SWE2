package applikation;

import gui.MainController;

public abstract class InputChecks {
	
	/**
	 * Checks if all the input fields are correct (and not null)
	 * @return		false if no error has ocurred, otherwise it sets an error message and returns true
	 */
	public static String kontaktError() {
		/*check fields for correct input*/
		/* if something is wrong set the error message and return true
		MainController.getInstance().setKontaktError("Some Error!"); */
		//return true;
		return null;
	}
	
	

}
